version: 2.1

orbs:
  go: circleci/go@3.0.4


jobs:
  build_and_test:
    working_directory: ./backend
    executor:
      name: go/default
      tag: '1.24'
    steps:
      - checkout
      - go/mod-download
      - go/test

workflows:
  main:
    jobs:
      - build_and_test
